import { InstructionService } from '../services/instruction.service';

const state = {
};

const actions = {
};

const mutations = {
    
};

export const instruction = {
    namespaced: true,
    state,
    actions,
    mutations
};
